# Third Dimension block palette v1 (P0)

Architect owns look and naming. Fabric owns registry and models.

Namespace: `brmc`. Creative tab: `brmc:building` (muted “Building”). The tab lists **primaries only** (Concrete, Light, Door, Door Frame for Third). Variants stay registered. Item names are mundane. No Third entities for P0. Wear is noise, not a map.

Cold lamp / sodium / wet concrete — hard break from Second crimson. Stall paint **lies** (misaligned / dead ends) and must not truthful-map. Ramp door is mouth literacy / liar — never honest EXIT or Overworld freedom bake. No Frontrooms sky in the body. No branded headlights. No attendant NPC. Fog is climate later; blocks stay empty-lot grammar. Debris / fill stays the concrete family, never a gray void tell.

Do **not** register P1 this pass: chainlink, crate, `headlight_prop`.

Textures in this pass are **TEMP** 16² from the Architect `third-p0` pack. `third_pipe` is the Clark design-disk exact PNG (no Adler-recover). Ramp-door upper pane is muted to opaque dark garage glass — no sky/outdoor read, no EXIT lettering. Regen Third chunks after pull.

## P0 registry

| Id | Visible name | Role |
|---|---|---|
| `third_concrete` | Concrete | Empty-lot field / fill / lid |
| `third_concrete_wet` | Concrete | Sparse wear |
| `third_pillar` | Pillar | Structural columns |
| `third_pipe` | Pipe | Infra noise |
| `third_fluorescent` | Light | Sodium fixture + hum-buzz BE stub |
| `third_fluorescent_dead` | Light | Unlit cousin + same BE |
| `third_stall_paint` | Paint | Lying stall noise — never a map |
| `third_drain` | Drain | Sparse floor wear |
| `third_door_ramp` | Door | Glance-cell liar mouth |
| `third_door_frame` | Door Frame | Mouth jambs |

First vestibule still goes to Second. Third is a dest-climate stub (`cold_garage`). No new live First / Second plane-cross this pass. `/brmc third` is the glance warp (moderator). Ramp door is not a threshold and not EXIT.
