package com.jamesstevenson.brmc.worldgen;

/**
 * Architecture can foreshadow these stacks. No player tutorial text.
 * Digital is sealed from First. Fourth is not a thread.
 */
public enum LoreThread {
	/** Apartment habitation lobe. Drywall / bed / kitchen. Not Bounded. */
	HABITATION_STACK,
	/** Utilities mouth and the rare janitor leak into Custodial. */
	MAINTENANCE_STACK,
	/** Commons teaches the false exit; vestibule is the true one. */
	EXIT_LITERACY_STACK,
	/** Curving hall: yellow continues around a soft plan into a nest. */
	CURVE_NEST,
	/** False floor / OOB hole: the fall that coils. */
	FALL_COIL,
	/** Vestibule door 2: leaving the First spine. */
	SPINE_LEAVE
}
